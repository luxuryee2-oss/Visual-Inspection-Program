# 로컬 vs 배포 환경 - 회원가입 작동 방식

## 🏠 로컬에서 테스트할 때

### Railway 필요 없음! ✅

로컬에서 테스트할 때는 **Railway가 전혀 필요 없습니다.**

1. **백엔드 서버 실행** (PC에서)
   ```bash
   cd backend
   npm run dev
   ```
   → `http://localhost:4000`에서 실행

2. **프론트엔드 서버 실행** (PC에서)
   ```bash
   cd frontend
   npm run dev
   ```
   → `http://localhost:5173`에서 실행

3. **브라우저에서 접속**
   - `http://localhost:5173` 접속
   - 회원가입/로그인 정상 작동 ✅

**결론**: 로컬에서는 Railway 없이도 완벽하게 작동합니다!

---

## 🌐 배포된 사이트에서 사용할 때

### 백엔드 서버가 필요함

배포된 사이트(Vercel)에서 회원가입을 사용하려면 **백엔드 서버가 인터넷에 배포되어 있어야 합니다.**

### 옵션 1: Railway (추천) ⭐
- 무료 티어 제공
- 설정 간단
- GitHub 연동

### 옵션 2: Render
- 무료 티어 제공
- Railway와 유사

### 옵션 3: 자체 서버
- AWS, Azure, GCP 등
- 자체 서버가 있다면 사용 가능

### 옵션 4: Vercel Serverless Functions
- Vercel에 백엔드도 함께 배포
- 단, SharePoint 연동이 복잡할 수 있음

---

## 🤔 언제 Railway가 필요한가?

### 필요 없는 경우 ✅
- 로컬 PC에서만 테스트
- 개발 중
- 내부 네트워크에서만 사용

### 필요한 경우 🌐
- 인터넷에 배포된 사이트에서 사용
- 모바일에서 접속
- 다른 사람이 사용
- 24시간 접근 가능해야 함

---

## 💡 현재 상황

### 지금 보고 있는 에러는?
- Vercel에 배포된 사이트에서 회원가입 시도
- 백엔드 서버가 없어서 405 에러 발생

### 해결 방법 2가지

#### 방법 1: 로컬에서 테스트 (Railway 불필요)
```bash
# 터미널 1
cd backend
npm run dev

# 터미널 2
cd frontend
npm run dev
```
→ `http://localhost:5173` 접속

#### 방법 2: 배포 환경에서 사용 (Railway 필요)
1. Railway에 백엔드 배포
2. Vercel 환경 변수 설정
3. 재배포

---

## 📊 비교표

| 환경 | Railway 필요? | 백엔드 위치 | 접속 방법 |
|------|-------------|-----------|---------|
| 로컬 개발 | ❌ 불필요 | PC (localhost:4000) | http://localhost:5173 |
| 배포 사이트 | ✅ 필요 | Railway/Render 등 | https://your-site.vercel.app |

---

## 🎯 추천

1. **지금 당장 테스트하고 싶다면**
   → 로컬에서 실행 (Railway 불필요)

2. **모바일에서도 사용하고 싶다면**
   → Railway에 백엔드 배포 필요

3. **둘 다 하고 싶다면**
   → 로컬에서 개발, Railway에 배포

